<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sizing.manthan.sizing"
    >


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Main"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Welcome"/>
        <activity android:name=".Ordering"/>
        <activity android:name=".FirstLevelClear"/>
        <activity android:name=".SecondLevelStart"/>
        <activity android:name=".SecondLevelSecond"/>
        <activity android:name=".SecondLevel"/>
        <activity android:name=".SecondFourth"/>

    </application>

</manifest>
